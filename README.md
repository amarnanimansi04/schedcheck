# AI Scheduling Constraint Solver

This project converts natural language scheduling problems into structured constraints using an LLM and solves them using Google OR-Tools CP-SAT.

Features:
- LLM constraint extraction
- Constraint validation + clarification loop
- Modular constraint handler system
- CP-SAT scheduling solver